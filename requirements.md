# シフト管理・出勤退勤管理アプリ 要件定義書

**作成日**: 2025年10月29日  
**バージョン**: 1.0

---

## 目次

1. [プロジェクト概要](#プロジェクト概要)
2. [システム構成](#システム構成)
3. [ユーザー役割](#ユーザー役割)
4. [機能要件](#機能要件)
5. [技術スタック](#技術スタック)
6. [データ構造](#データ構造)
7. [画面一覧](#画面一覧)
8. [計算ロジック](#計算ロジック)

---

## プロジェクト概要

### 目的
町民にアプリテストの仕事を依頼する際の、シフト管理・出退勤記録・給与計算を一元管理し、管理者の負担を軽減しミスをなくす。

### 現状の課題
- シフト管理: oplusを使用
- 出退勤管理: 管理者が手動で記録
- 給与計算: スプレッドシートで計算

これらをバラバラに運用しているため、手間がかかりミスが発生しやすい。

### 解決策
シフト管理→出退勤記録→給与計算までをシームレスに行える単一システムの構築

### 利用規模
- 初期: テスター10名
- 今後増加予定
- 拠点: 1箇所

---

## システム構成

### プラットフォーム
- **PWA (Progressive Web App)**
  - ブラウザベースのWebアプリケーション
  - スマホのホーム画面に追加可能
  - オフライン対応（一部機能）

### デバイス対応
- **テスター**: スマートフォン（iOS/Android）
- **管理者**: PC/タブレット
- **打刻用**: iPad（カメラ機能使用）

---

## ユーザー役割

### 管理者
- シフトの作成・承認・編集
- 出退勤データの確認・修正
- 給与計算・確認
- テスター管理（追加・削除・編集）
- 案件管理
- マスタ設定

### テスター
- シフト希望の提出
- 確定シフトの確認
- QRコードでの出勤・退勤打刻
- 自分の勤務実績・給与の確認

---

## 機能要件

### 1. 認証機能

#### ログイン
- メールアドレスとパスワードによる認証
- 管理者とテスターで異なる画面に遷移

#### テスター登録フロー
1. 管理者が新規テスター情報を登録（名前、メールアドレス、電話番号）
2. テスターに招待メールが自動送信
3. テスターが招待リンクからパスワードを設定
4. アカウント作成完了

---

### 2. 案件管理機能（管理者のみ）

#### 案件登録
- **管理項目**:
  - 案件名（必須）
  - 期間: 開始日〜終了日（必須）
  - 基本時給（必須）※シフト作成時に個別調整可能
  - 必要人数（必須）※人数ではなく「必要総労働時間（人×時間）」として使用

#### 案件操作
- 新規登録
- 編集
- 削除
- 一覧表示

---

### 3. シフト管理機能

#### 3.1 テスター側機能

##### シフト希望提出
- **提出内容**:
  - 日付選択
  - 開始時間・終了時間選択（9:00-18:00の範囲内）
  - 1日1つの時間帯のみ
- **提出期間**: 制限なし（いつでも提出可能）
- **編集・取消**: いつでも可能
  - ※運用ルール: 編集・取消時は管理者に連絡必須

##### 確定シフト確認
- **表示形式**: カレンダー形式
- **表示内容**:
  - 日付
  - 時間（開始〜終了）
  - 案件名
  - 稼働予定時間

#### 3.2 管理者側機能

##### シフト希望確認
- テスター別の希望一覧
- 日付別の希望一覧

##### シフト作成・確定
- **フロー**:
  1. テスターの希望を確認
  2. 希望を承認して以下を設定:
     - 案件の割り当て
     - 時給の設定（案件の基本時給がデフォルト、個別調整可能）
  3. シフト確定

##### シフト確認画面
- **表示形式**: カレンダー形式
  - 月表示 / 週表示 / 日表示の切替可能
  - 横軸: 日付
  - 縦軸: テスター

- **各セルの表示内容**:
  - 案件名
  - 時間（例: 9:00-18:00）
  - 稼働予定時間（例: 8時間）

- **案件別集計表示**（同画面内）:
  - 案件名
  - 必要時間（例: 80時間）
  - 確定済み（例: 5人×8時間 = 40時間）
  - 残り（例: 40時間）

##### シフト編集
- 確定済みシフトの変更・削除が可能
  - 時間変更
  - 案件変更
  - 時給変更
  - シフト削除

---

### 4. 出退勤管理機能

#### 4.1 テスター側機能

##### QRコード表示
- マイページに個人用QRコード常時表示
- 打刻時に使用

##### 打刻履歴確認
- **表示形式**: カレンダー形式 + リスト形式
- **表示内容**:
  - 日付
  - 出勤時刻（正式記録）
  - 退勤時刻（正式記録）
  - 稼働時間

#### 4.2 iPad打刻画面

##### 打刻フロー
1. テスターがスマホでQRコードを表示
2. iPadのカメラでQRコードを読み取り
3. iPad画面に「出勤」「退勤」ボタンが表示
4. いずれかを選択
5. 打刻完了画面を表示（2〜3秒）:
   - テスター名
   - 打刻時刻
   - 出勤/退勤
   - 「打刻完了しました」メッセージ
6. 自動的にQRコード読み取り待機画面に戻る

#### 4.3 管理者側機能

##### 打刻記録確認
- **表示形式**: テスター別 / 日別の両方
- **表示内容**:
  - 日付
  - 打刻時刻（参考データとして保持）
  - 正式記録時刻（給与計算に使用）
  - 稼働時間

##### 打刻時刻の処理
- **15分単位の丸め込み**:
  - 出勤: 切り捨て（例: 9:07 → 9:00）
  - 退勤: 切り上げ（例: 17:07 → 17:15）
- 丸め込み後の時刻が「正式記録時刻」としてデフォルト入力される
- 管理者が必要に応じて修正可能

##### 手動記録・修正
- 打刻忘れの記録（出勤・退勤時刻の手動入力）
- 正式記録時刻の修正
- 遅刻・早退・欠勤の対応

##### 複数案件の処理
- 同日に複数案件がある場合:
  - 出勤・退勤は1日2回のみ打刻
  - シフト情報を元に各案件の勤務時間を自動振り分け
  - 管理者が必要に応じて手動で調整可能

---

### 5. 給与計算機能

#### 5.1 計算ロジック

##### 休憩時間の扱い
- 勤務時間が **12:00-13:00を跨ぐ場合のみ** 休憩1時間を引く
- 例:
  - 9:00-18:00 → 休憩1時間引く → 稼働8時間
  - 9:00-12:00 → 休憩引かない → 稼働3時間
  - 13:00-18:00 → 休憩引かない → 稼働5時間

##### 給与計算
```
稼働時間 = 退勤時刻 - 出勤時刻 - 休憩時間（該当する場合のみ1時間）
給与 = 稼働時間 × 時給
```

##### 端数処理
- 出退勤記録が15分単位なので、稼働時間も自動的に15分単位
- 給与は1円単位で計算

##### 締め日
- 月末締め
- 支払いは翌月末（支払い処理は別システムで実施、本システムは計算まで）

#### 5.2 管理者側機能

##### 個人別給与確認
- **月別全体集計**:
  - 総勤務日数
  - 総勤務時間
  - 総給与

- **案件別集計**:
  - 案件名
  - 勤務日数
  - 勤務時間
  - 給与

- **日別詳細**:
  - 日付
  - 案件名
  - 時給
  - 勤務時間
  - 給与

##### 案件別給与確認
- **案件情報**:
  - 案件名
  - 期間
  - 基本時給（参考）

- **参加テスター一覧**:
  - テスター名
  - 勤務日数
  - 勤務時間
  - 時給（個別設定の場合）
  - 給与

- **案件全体の合計人件費**

##### 月次レポート
- **全テスターの給与一覧**:
  - テスター名
  - 総勤務日数
  - 総勤務時間
  - 総給与

- **案件別集計**:
  - 案件名
  - 合計人件費

#### 5.3 テスター側機能

##### 自分の給与確認
- 月別集計
- 案件別集計
- 日別詳細
- ※全て閲覧可能

---

### 6. テスター管理機能（管理者のみ）

#### テスター登録
- **登録情報**:
  - 名前（必須）
  - メールアドレス（必須）
  - 電話番号（必須）

#### テスター編集
- 基本情報の変更

#### ステータス管理
- **アクティブ**: 通常のテスター
- **休止**: 辞めた/休んでいるテスター
  - ログイン不可
  - シフト希望提出不可
  - 管理者も新規シフトに割り当て不可
  - 過去のシフトや給与データは閲覧可能（本人・管理者とも）

#### テスター削除
- データは物理削除ではなく、ステータスを「休止」に変更することを推奨

---

### 7. マスタ管理機能（管理者のみ）

#### 休憩時間設定
- 現在のデフォルト: 12:00-13:00
- 管理者が変更可能
- 変更後は全体に適用

---

### 8. ダッシュボード機能

#### 8.1 管理者用ダッシュボード
- 今日のシフト一覧
- 今日の出勤状況（誰が打刻済みか）
- 今週/今月のシフト概要
- 未承認のシフト希望件数

#### 8.2 テスター用ダッシュボード
- 今日のシフト（自分の勤務予定）
- 今週/今月のシフト一覧

---

## 技術スタック

### フロントエンド
- **Next.js** - Reactベースのフルスタックフレームワーク
- **TypeScript** - 型安全性の確保
- **Tailwind CSS** - UIスタイリング

### バックエンド・インフラ
- **Firebase**
  - **Firestore**: NoSQLデータベース
  - **Firebase Authentication**: 認証機能
  - **Firebase Storage**: 画像等の保存（必要に応じて）
  - **Firebase Hosting**: PWAのホスティング

### その他
- **PWA対応**: Service Worker、manifest.json
- **QRコード**: カメラAPI、QRコード生成/読み取りライブラリ

---

## データ構造

### Users（ユーザー）
```
{
  id: string,
  name: string,
  email: string,
  phone: string,
  role: 'admin' | 'tester',
  status: 'active' | 'inactive',
  createdAt: timestamp,
  updatedAt: timestamp
}
```

### Projects（案件）
```
{
  id: string,
  name: string,
  startDate: date,
  endDate: date,
  baseHourlyWage: number,
  requiredHours: number,
  createdAt: timestamp,
  updatedAt: timestamp
}
```

### ShiftRequests（シフト希望）
```
{
  id: string,
  testerId: string,
  date: date,
  startTime: time,
  endTime: time,
  status: 'pending' | 'approved' | 'rejected',
  createdAt: timestamp,
  updatedAt: timestamp
}
```

### Shifts（確定シフト）
```
{
  id: string,
  testerId: string,
  projectId: string,
  date: date,
  startTime: time,
  endTime: time,
  hourlyWage: number,
  expectedWorkHours: number,
  createdAt: timestamp,
  updatedAt: timestamp
}
```

### Attendances（出退勤記録）
```
{
  id: string,
  testerId: string,
  shiftId: string,
  date: date,
  
  // 打刻時刻（参考データ）
  clockInTime: timestamp,
  clockOutTime: timestamp,
  
  // 正式記録時刻（給与計算用）
  recordedClockInTime: timestamp,
  recordedClockOutTime: timestamp,
  
  // 勤務時間
  workHours: number,
  breakHours: number,
  actualWorkHours: number,
  
  createdAt: timestamp,
  updatedAt: timestamp
}
```

### Settings（システム設定）
```
{
  id: string,
  breakStartTime: time,  // 例: 12:00
  breakEndTime: time,    // 例: 13:00
  updatedAt: timestamp
}
```

---

## 画面一覧

### 共通
1. ログイン画面
2. パスワード設定画面（初回登録時）

### テスター向け
1. ダッシュボード
2. シフト希望提出画面
3. シフト確認画面（カレンダー）
4. 打刻用QRコード表示画面
5. 出退勤履歴確認画面（カレンダー + リスト）
6. 給与確認画面
   - 月別集計
   - 案件別集計
   - 日別詳細

### 管理者向け
1. ダッシュボード
2. 案件管理画面
   - 案件一覧
   - 案件登録/編集
3. シフト管理画面
   - シフト希望一覧
   - シフト承認画面
   - シフトカレンダー（月/週/日）
4. 出退勤管理画面
   - 出退勤記録一覧（テスター別/日別）
   - 手動記録/修正画面
5. 給与管理画面
   - 個人別給与確認
   - 案件別給与確認
   - 月次レポート
6. テスター管理画面
   - テスター一覧
   - テスター登録/編集
7. マスタ設定画面
   - 休憩時間設定

### iPad専用
1. QRコード読み取り画面
2. 打刻選択画面（出勤/退勤）
3. 打刻完了画面

---

## 計算ロジック

### 稼働予定時間の計算
```typescript
function calculateExpectedWorkHours(
  startTime: string,
  endTime: string,
  breakStart: string = '12:00',
  breakEnd: string = '13:00'
): number {
  const start = parseTime(startTime);
  const end = parseTime(endTime);
  let totalHours = (end - start) / (1000 * 60 * 60);
  
  // 12:00-13:00を跨ぐ場合は1時間引く
  if (start < parseTime(breakEnd) && end > parseTime(breakStart)) {
    totalHours -= 1;
  }
  
  return totalHours;
}
```

### 出退勤時刻の丸め込み
```typescript
function roundClockInTime(time: Date): Date {
  // 15分単位で切り捨て
  const minutes = time.getMinutes();
  const roundedMinutes = Math.floor(minutes / 15) * 15;
  return new Date(time.setMinutes(roundedMinutes, 0, 0));
}

function roundClockOutTime(time: Date): Date {
  // 15分単位で切り上げ
  const minutes = time.getMinutes();
  const roundedMinutes = Math.ceil(minutes / 15) * 15;
  return new Date(time.setMinutes(roundedMinutes, 0, 0));
}
```

### 実稼働時間の計算
```typescript
function calculateActualWorkHours(
  clockIn: Date,
  clockOut: Date,
  breakStart: string = '12:00',
  breakEnd: string = '13:00'
): number {
  const workMinutes = (clockOut.getTime() - clockIn.getTime()) / (1000 * 60);
  let workHours = workMinutes / 60;
  
  // 12:00-13:00を跨ぐ場合は1時間引く
  if (clockIn < parseTime(breakEnd) && clockOut > parseTime(breakStart)) {
    workHours -= 1;
  }
  
  return workHours;
}
```

### 給与計算
```typescript
function calculateWage(workHours: number, hourlyWage: number): number {
  return workHours * hourlyWage;
}
```

---

## 非機能要件

### セキュリティ
- Firebase Authenticationによる認証
- Firestore Security Rulesによるアクセス制御
  - テスターは自分のデータのみ閲覧可能
  - 管理者は全データにアクセス可能
- HTTPS通信の使用

### パフォーマンス
- ページ読み込み時間: 3秒以内
- 打刻処理: 1秒以内に完了

### 可用性
- Firebase による 99.9% の稼働率を目標

### スケーラビリティ
- 将来的に100名程度まで拡張可能な設計

---

## 今後の拡張可能性

以下の機能は初期リリースでは実装せず、将来的に検討する：

- 通知機能（アプリ内通知、メール通知）
- データエクスポート機能（CSV/Excel）
- チャット機能（テスター⇔管理者）
- 複数拠点対応
- 給与明細のPDF出力
- スマホアプリ版（ネイティブアプリ）

---

## 運用ルール

以下は運用上のルールとして、システム外で管理する：

1. シフト希望の編集・取消時は管理者に連絡
2. 打刻忘れは管理者に連絡
3. 遅刻・早退・欠勤は管理者に連絡
4. シフトや給与に関する連絡はLINEで実施

---

## 開発の優先順位

### Phase 1（MVP）
1. 認証機能
2. テスター管理
3. 案件管理
4. シフト管理（希望提出〜承認）
5. 出退勤管理（QRコード打刻）
6. 給与計算（基本）

### Phase 2
1. ダッシュボード
2. 詳細な給与レポート機能
3. マスタ管理機能
4. UIの改善

### Phase 3
1. PWA最適化
2. パフォーマンス改善
3. 拡張機能の検討

---

## 見積もり

### 開発期間
- Phase 1（MVP）: 4-6週間
- Phase 2: 2-3週間
- Phase 3: 1-2週間

### コスト
- 開発費: 開発者による（自己開発の場合は0円）
- インフラ費用（Firebase）:
  - 初期: 無料
  - 月額: 10名規模で0〜500円程度
- ドメイン費用: 年間1,000〜2,000円

---

**以上**
